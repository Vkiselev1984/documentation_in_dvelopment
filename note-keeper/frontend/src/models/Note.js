class Note {
    constructor(id, title, content, categoryId) {
        this.id = id;
        this.title = title;
        this.content = content;
        this.categoryId = categoryId;
    }
}

export default Note;
